<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable name="callback" type="org.cru.godtools.tutorial.util.OnBoardingCallbacks" />
    </data>

    <merge>

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/onboarding_previous_button"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_marginStart="16dp"
            android:layout_marginLeft="16dp"
            android:layout_marginTop="16dp"
            android:onClick="@{() -> callback.onPreviousClicked()}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_nav_prev_arrow"
            app:tint="@color/gt_blue" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/onboarding_close_button"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginRight="16dp"
            android:onClick="@{() -> callback.onCloseClicked()}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_close"
            app:tint="@color/gt_blue" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/onboarding_next_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="32dp"
            android:onClick="@{() -> callback.onNextClicked()}"
            style="@style/Widget.GodTools.Button.Borderless.Colored.Blue"
            android:text="@string/on_boarding_continue"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </merge>
</layout>
