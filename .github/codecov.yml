codecov:
  notify:
    after_n_builds: 2

coverage:
  status:
    project:
      default:
        threshold: 1
    patch:
      default:
        target: 0%
        threshold: 1
github_checks:
  annotations: false
