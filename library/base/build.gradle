dependencies {
    implementation "com.android.support:support-annotations:${deps.androidSupport}"
    implementation "com.android.support:support-compat:${deps.androidSupport}"

    implementation "org.ccci.gto.android:gto-support-compat:${deps.gtoSupport}"

    implementation "com.google.guava:guava:${deps.guava}"
    implementation "me.thekey.android:thekey-api:${deps.thekey}"

    testImplementation "junit:junit:${deps.junit}"
    testImplementation "org.hamcrest:hamcrest-library:${deps.hamcrest}"
}
