dependencies {
    implementation project(':library:model')

    implementation "com.android.support:support-annotations:${deps.androidSupport}"

    api "org.ccci.gto.android:gto-support-db:${deps.gtoSupport}"
    api "org.ccci.gto.android:gto-support-db-async:${deps.gtoSupport}"
    api "org.ccci.gto.android:gto-support-db-stream:${deps.gtoSupport}"

    implementation "com.crashlytics.sdk.android:crashlytics:${deps.crashlytics}"

    // Room components
//    implementation "android.arch.persistence.room:runtime:${deps.roomVersion}"
//    annotationProcessor "android.arch.persistence.room:compiler:${deps.roomVersion}"
//    androidTestImplementation "android.arch.persistence.room:testing:${deps.roomVersion}"
//
//    // Lifecycle components
//    implementation "android.arch.lifecycle:extensions:${deps.archLifecycleVersion}"
//    annotationProcessor "android.arch.lifecycle:compiler:${deps.archLifecycleVersion}"
}
