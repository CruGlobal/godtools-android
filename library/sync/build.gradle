dependencies {
    implementation project(':library:api')
    implementation project(':library:db')
    implementation project(':library:model')

    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:${deps.kotlinCoroutines}"

    implementation "androidx.collection:collection-ktx:${deps.androidX.collection}"
    implementation "androidx.core:core-ktx:${deps.androidX.core}"
    api "androidx.hilt:hilt-work:${deps.androidX.hilt}"
    api "androidx.work:work-runtime-ktx:${deps.androidX.work}"

    implementation "org.ccci.gto.android:gto-support-base:${deps.gtoSupport}"
    api "org.ccci.gto.android:gto-support-sync:${deps.gtoSupport}"

    implementation "com.google.dagger:dagger:${deps.dagger}"
    implementation "com.google.dagger:hilt-android:${deps.hilt}"
    implementation "org.greenrobot:eventbus:${deps.eventbus}"

    testImplementation "org.jetbrains.kotlinx:kotlinx-coroutines-test:${deps.kotlinCoroutines}"

    kapt "androidx.hilt:hilt-compiler:${deps.androidX.hilt}"
    kapt "com.google.dagger:dagger-compiler:${deps.dagger}"
    kapt "com.google.dagger:hilt-compiler:${deps.hilt}"
}
