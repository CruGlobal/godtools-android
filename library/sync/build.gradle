apply plugin: 'kotlin-android'

dependencies {
    implementation project(':library:base')
    implementation project(':library:sync-tasks')

    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:${deps.kotlin}"

    implementation "org.ccci.gto.android:gto-support-base:${deps.gtoSupport}"
    // HACK: temporarily include the core dependency because sync doesn't correctly set the dependency as an api
    //       transitive dependency
    implementation "org.ccci.gto.android:gto-support-core:${deps.gtoSupport}"
    implementation "org.ccci.gto.android:gto-support-sync:${deps.gtoSupport}"

    implementation "com.evernote:android-job:${deps.evernoteAndroidJob}"
    implementation "org.greenrobot:eventbus:${deps.eventbus}"
}
